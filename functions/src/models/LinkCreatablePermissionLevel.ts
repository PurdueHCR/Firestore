/**
 * Who has the permissions to create a Link using this Point Type.
 */
export enum LinkCreatablePermissionLevel {
    PROFFESSIONAL_STAFF_ONLY = 1,
    RESIDENTIAL_STAFF_ONLY = 2,
    ALL_LINK_CREATORS = 3
}